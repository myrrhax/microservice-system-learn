server:
  port: 8082

spring:
  application:
    name: ingestion-service

  kafka:
    bootstrap-servers: localhost:9094

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/eureka/

app:
  simulation:
    requests-per-interval: 50
    interval-ms: 5000