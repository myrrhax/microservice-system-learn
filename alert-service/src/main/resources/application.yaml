spring:
  application:
    name: alert-service

  kafka:
    bootstrap-servers: localhost:9094
    consumer:
      group-id: alert-service

  datasource:
    hikari:
      schema: microservices_schema
      connection-init-sql: CREATE SCHEMA IF NOT EXISTS microservices_schema
      connection-timeout: 10000

server:
  port: 8084